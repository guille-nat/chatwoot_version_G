#!/bin/sh

# script prevents pushing to master by mistake
# Fork note: upstream also blocks develop here, but develop is this fork's
# mainline branch, so that guard is intentionally removed.

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "master" ]; then
  echo "You can't push directly to master branch"
  exit 1
fi
